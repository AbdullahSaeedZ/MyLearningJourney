#include <iostream>
using namespace std;


// Encapsulation: bundling data (variables) and methods inside a single class.
// Purpose:
//   - Control modification of data.
//   - Provide read-only / write-only access via getters & setters.
//   - Achieve data hiding by making members private or protected.

 
// In OOP: access to an object’s data is only through its methods.
// encapsulation leads to abstraction, and the way that we only reach the class members only via the object, this is encapsulation.
// Note: Encapsulation ≠ Data Hiding
//   - Encapsulation = grouping fields + methods.
//   - Data Hiding = restricting access.




int main()
{
    
    
    
    
    
    return 0;
}

