-- when typing queris, which database are we using ?
-- in this SQL server there is mulitple databases we created
-- by default it is master database for the system, we can see on the top, master, we can change it or
-- we use this command to specify which database are we quering:

use DB1; -- now all following queris scripts(all query lines) will be applied on DB1
