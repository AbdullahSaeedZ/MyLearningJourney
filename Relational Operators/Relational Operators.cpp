#include <iostream>
using namespace std;

int main()
{
    short int A = 10, B = 20;

    // rational operators are used for comarison of variables as the following:

    cout << (A == B) << endl;
    cout << (A != B) << endl;
    cout << (A > B) << endl;
    cout << (A < B) << endl;
    cout << (A >= B) << endl;
    cout << (A <= B) << endl;

    // output is true(1) or false(0) depending on operator used
    
    
    
    return 0;
}

